{
  "$schema_comment": "qcc 企查查 MCP 配置。把这个文件复制成 qcc_config.json 后填入你从 `claude mcp get qcc-company` / `claude mcp get qcc-risk` 拿到的 URL 与 Bearer token。qcc_config.json 已 gitignore，不会被提交。",
  "transport": "http",
  "timeout_seconds": 60,
  "servers": {
    "qcc-company": {
      "url": "https://agent.qcc.com/mcp/company/stream",
      "auth_bearer": "你的 qcc-company Bearer token"
    },
    "qcc-risk": {
      "url": "https://agent.qcc.com/mcp/risk/stream",
      "auth_bearer": "你的 qcc-risk Bearer token"
    }
  }
}
